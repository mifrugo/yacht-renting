  <% @yachts.each do |yacht| %>
     <%= @yacht.title%>
     <%= link_to "Show", yacht_path(yacht) %>
     <% if policy(yacht).edit? %>
        <%= link_to "Edit", yachts_path(yacht) %>
     <% end %>
     <% if policy(yacht).destroy? %>
 <%= link_to "Delete", yacht method: :delete, data: { confirm:"Are you sure?"} %>
     <% end %>
 <% end %>

<% if policy(Yacht).new? %>
<%= link_to "Create new listing", yachts_new_path %>
<% end %>
