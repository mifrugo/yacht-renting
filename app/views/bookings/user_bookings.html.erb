<ul class="nav nav-pills mb-3 justify-content-center" id="pills-tab" role="tablist">
  <li class="nav-item" role="presentation">
    <a class="nav-link active" id="pills-profile-tab" data-toggle="pill" href="#pills-profile" role="tab" aria-controls="pills-profile" aria-selected="false">Upcoming</a>
  </li>
  <li class="nav-item" role="presentation">
    <a class="nav-link " id="pills-home-tab" data-toggle="pill" href="#pills-home" role="tab" aria-controls="pills-home" aria-selected="true">Past</a>
  </li>
</ul>
<div class="tab-content" id="pills-tabContent">
  <div class="tab-pane fade" id="pills-home" role="tabpanel" aria-labelledby="pills-home-tab">
    <% unless @past.empty? %>
      <div class="card-columns text-center">
       <% @past.each do |e| %>
         <%= render 'booking_cards', locals: e %>
    <% end %>
      </div>
    <% else %>
      <h5  class="text-center pt-3">Still waiting for you first trip!</h5>
    <% end %>
  </div>
  <div class="tab-pane fade active show" id="pills-profile" role="tabpanel" aria-labelledby="pills-profile-tab">
    <% unless @upcoming.empty? %>
      <div class="card-columns text-center">
       <% @upcoming.each do |e| %>
         <%= render 'booking_cards', locals: e %>
    <% end %>
      </div>
    <% else %>
      <h5 class="text-center pt-3">No upcoming trips, what are you waiting for?</h5>
    <% end %>
    </div>
  </div>
</div>

<style type="text/css">
  @media screen and (max-width: 600px) {
   .step-item {
      margin: 5px;
   }
   .step-item > #besi {
    margin: 0px, 15px;
   }
}
</style>
